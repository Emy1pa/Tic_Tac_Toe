// Foreach to display
const fruits = ["apple", "banane", "orange"];

fruits.forEach((fruit, key) => {
  console.log(`${key}: ${fruit}`);
});

// for of to display elements

// change elements in the table map
// filter
